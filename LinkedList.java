public class LinkedList {
    Node head; // list head
    //node - linkedlist
    class Node {
        int data;
        Node next;
        //constructor to create a new node
        Node(int d) { data = d; }
    }
}
